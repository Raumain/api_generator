{
	"name": "elysia_template",
	"version": "1.0.50",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "bun run --watch src/index.ts",
		"fmt": "pnpm biome format --write . && pnpm biome lint --write ."
	},
	"dependencies": {},
	"devDependencies": {},
	"module": "src/index.js"
}
